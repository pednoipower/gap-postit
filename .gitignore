# runtime data written by the fallback server during a workshop
server/workshop-data.json

.DS_Store

# survey results and other source documents stay off the public repo
*.pdf

# room backups taken before clearing: real answers, never in the repo
backups/
